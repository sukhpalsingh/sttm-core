{
  "name": "sttm3-html",
  "version": "1.0.0",
  "description": "The HTML for SikhiToTheMax 3, to be used as a submodule for the desktop and mobile apps",
  "scripts": {
    "build-css": "node-sass --output-style compressed --source-map true --include-path scss scss/foundation.scss css/bundle.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
  },
  "author": "Khalis Foundation",
  "devDependencies": {
    "node-sass": "^3.5.3",
    "nodemon": "^1.9.1"
  },
  "dependencies": {
    "marked": "^0.3.6"
  }
}
